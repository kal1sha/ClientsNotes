<h1>Editing post</h1>
 
<%= form_for :people, :url => { :action => :update, :id => @people.id },
:method => :put do |f| %>
  <% if @people.errors.any? %>
  <div id="errorExplanation">
    <h2><%= pluralize(@people.errors.count, "error") %> prohibited
        this people from being saved:</h2>
    <ul>
    <% @people.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
  <p>
    <%= f.label :secondname %><br>
    <%= f.text_field :secondname %>
  </p>
 
  <p>
    <%= f.label :firstname %><br>
    <%= f.text_area :firstname %>
  </p>

  <p>
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </p>

  <p>
    <%= f.submit %>
  </p>
<% end %>
 
<%= link_to 'Back', :action => :index %>